# Core dependencies for word grid generation
jinja2==3.1.6

# Google Chrome required for PDF generation (install separately)

# Optional dependencies for AI image generation (generate_wordlist_images.py)
webuiapi==0.9.17
tqdm==4.67.1
requests==2.32.4